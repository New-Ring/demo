server:
  port: 8082
  servlet:
    context-path: /demo

spring:
  profiles:
    active: dev

logging:
  config: classpath:logback-spring.xml
  level:
    dao: debug
    org:
      mybatis: debug


---
person:
  name: 张三${random.uuid}
  age: ${random.int}
  tellPhone: ${person.age}
  sex: true
  map: {key:value}
  birth: 1993/10/28
  list:
    - ${person.age}
    - 2
    - 3


